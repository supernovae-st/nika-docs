---
title: "Per-client MCP wiring"
description: "One stanza, every client: Cursor, Claude Code, Codex, Zed, Gemini CLI, Windsurf, Continue, Cline, Aider — one dedicated page per tool."
sidebarTitle: "All clients"
icon: "plug-circle-bolt"
---

Every MCP-capable client wires the same read-only oracle
([what it exposes](/reference/mcp-server)) with the same two fields —
`command: nika` · `args: [mcp]`. The binary comes first:
`brew install supernovae-st/tap/nika` ([other paths](/getting-started/installation)).
This page is the matrix; every client has a dedicated page with the exact
file, shape, reload behavior and verify step.

One-click where the client supports it (binary still required first):

[![Install in VS Code](https://img.shields.io/badge/VS_Code-install%20nika%20mcp-0098FF?logo=githubcopilot)](https://insiders.vscode.dev/redirect/mcp/install?name=nika&config=%7B%22command%22%3A%20%22nika%22%2C%20%22args%22%3A%20%5B%22mcp%22%5D%7D)
[![Install MCP Server](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en/install-mcp?name=nika&config=eyJjb21tYW5kIjogIm5pa2EiLCAiYXJncyI6IFsibWNwIl19)

And in every case, `nika init` first — the agent-teaching surface
(`AGENTS.md`, rules, the repo skill) is what makes the oracle useful.

## The client matrix

| Client | Wire | Where it lands |
|---|---|---|
| [Claude Code](/integrations/clients/claude-code) | `nika wire claude` | `~/.claude.json` |
| [Claude Desktop](/integrations/clients/claude-desktop) | `nika wire claude-desktop` | `claude_desktop_config.json` (app-config dir) |
| [VS Code / Copilot](/integrations/clients/vscode) | `nika wire vscode` | `.vscode/mcp.json` (repo-local) |
| [Cursor](/integrations/clients/cursor) | `nika wire cursor` | `~/.cursor/mcp.json` |
| [Windsurf](/integrations/clients/windsurf) | `nika wire windsurf` | `~/.codeium/windsurf/mcp_config.json` |
| [Zed](/integrations/clients/zed) | `nika wire zed` | `~/.config/zed/settings.json` |
| [Codex](/integrations/clients/codex) | `nika wire codex` | `~/.codex/config.toml` |
| [OpenCode](/integrations/opencode) | `nika wire opencode` | `opencode.json` (repo-local) |
| [Hermes](/integrations/hermes) | `nika wire hermes` | `~/.hermes/config.yaml` |
| [Gemini CLI](/integrations/clients/gemini-cli) | `nika wire gemini` | `~/.gemini/settings.json` |
| [Qwen Code](/integrations/clients/qwen-code) | `nika wire qwen` | `~/.qwen/settings.json` |
| [Cline](/integrations/clients/cline) | `nika wire cline` | `cline_mcp_settings.json` (resolved) |
| [Continue](/integrations/clients/continue) | `nika wire continue` | `~/.continue/mcpServers/nika.json` |
| [LM Studio](/integrations/clients/lm-studio) | `nika wire lmstudio` | `~/.lmstudio/mcp.json` (resolved) |
| [Junie](/integrations/clients/junie) | `nika wire junie` | `.junie/mcp/mcp.json` (repo-local) |
| [goose](/integrations/clients/goose) | manual stanza | goose config `extensions:` |
| [Open WebUI](/integrations/clients/open-webui) | manual — mcpo recipe | OpenAPI tool server |
| [AnythingLLM](/integrations/clients/anythingllm) | manual stanza | its `mcpServers` file |
| [Cherry Studio](/integrations/clients/cherry-studio) | manual stanza | settings UI (stdio) |
| [Jan](/integrations/clients/jan) | manual stanza | settings UI (stdio) |
| [Aider](#aider) | no MCP — `AGENTS.md` covers it | `.aider.conf.yml` |
Every `nika wire` write is idempotent (re-run prints `· current`) and
preserves unrelated servers.

The anchors below stay for directory deep-links — each points to its
dedicated page.

## Cursor

`nika wire cursor` → `~/.cursor/mcp.json`, plus the one-click deeplink and
the auto-attached `.cursor/rules/nika.mdc` —
[full page](/integrations/clients/cursor).

## Claude Code

`nika wire claude` → `~/.claude.json`, plus the first-party plugin
(`claude plugin install nika@nika`) —
[full page](/integrations/clients/claude-code).

## Codex

`nika wire codex` → `~/.codex/config.toml` (comments preserved), plus the
plugin — [full page](/integrations/clients/codex).

## VS Code / Copilot

`nika wire vscode` → repo-local `.vscode/mcp.json`, plus the marketplace
extension — [full page](/integrations/clients/vscode).

## Zed

`nika wire zed` → `context_servers` in `~/.config/zed/settings.json`, with
the JSONC-respecting contract —
[full page](/integrations/clients/zed).

## Gemini CLI

The `mcpServers` stanza in `~/.gemini/settings.json` (wire lands next
release) — [full page](/integrations/clients/gemini-cli).

## Windsurf

`nika wire windsurf` → `~/.codeium/windsurf/mcp_config.json` —
[full page](/integrations/clients/windsurf).

## OpenCode

Native `AGENTS.md` + repo-skill discovery — see the dedicated
[OpenCode page](/integrations/opencode).

## Hermes

Tap-installable delegation skill + `mcp_servers` stanza — see the dedicated
[Hermes page](/integrations/hermes).

## Continue

An own-file drop: `~/.continue/mcpServers/nika.json` — reload to pick it
up — [full page](/integrations/clients/continue).

## Cline

`cline_mcp_settings.json` (hot-reloaded; the file's two possible homes
resolved) — [full page](/integrations/clients/cline).

## LM Studio

Cursor-notation `mcp.json`, with the real macOS path quirk —
[full page](/integrations/clients/lm-studio).

## goose

A stdio extension stanza + native `AGENTS.md` reading —
[full page](/integrations/clients/goose).

## Aider

Aider reads repo conventions rather than MCP: `nika init`'s `AGENTS.md`
covers it (add `CONVENTIONS.md: AGENTS.md` to `.aider.conf.yml` if you keep
conventions elsewhere). The check loop is the same:
`nika check <file>` after every edit.

## Verify (any client)

Ask the agent to list its tools — the nine `nika_*` names should appear.
Then prove the loop end-to-end:
[integrate in 10 minutes](/integrations/quickstart).

> Client config formats move fast. The stable contract is the stanza
> (`nika` + `mcp`) and the oracle's read-only surface — if a client's
> documented path has moved, the stanza still applies.
