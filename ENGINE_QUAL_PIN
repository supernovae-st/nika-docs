# Migrated-engine qualification pin for pre-release docs CI.
# The released-engine jobs (install_release.py) remain the visitor-surface
# judge and are not this file. Qualification builds nika from this exact
# commit and runs oracle-sweep / arm plants against that binary.
# Bump deliberately when the engine worker publishes a new SHA.
718ca3ca36f82040bdb3d5450c7b1f80599ca41b
