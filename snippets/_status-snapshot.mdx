{/* _status-snapshot.mdx — MIXED PROVENANCE. Read this before editing.

    AUTO-DERIVED by scripts/mintlify-snapshot.sh: `version` + `lastUpdated` ONLY
    (from the published GitHub release). Everything else below is a HAND-MAINTAINED
    periodic snapshot of engine `main` — the script never touches those lines, so a
    green `mintlify-snapshot` run proves NOTHING about the counts.

    NOT GUARDED: engine hygiene vector 23 (check-status-claims-sync.sh) does NOT
    cover this file. Its whitelist is `ROADMAP.md` + `.claude/CLAUDE.md`, resolved
    against the ENGINE repo root — it cannot see this repo. Refresh by hand.

    Counts are as of engine main `79588ad76` (2026-07-23) — the commit at which the
    canonical `scripts/refresh-status.sh` block in engine `.claude/CLAUDE.md` was
    last regenerated. crates/tests/clippy are read from that block. adrs / providers
    / capabilityRules / hygieneVectors were re-derived at engine main tip
    `af286eb6f` (2026-07-27) and are current there.

    Derivation, one command per field (run from the engine checkout):
      head · cratesWorkspace · cratesAdmitted · libTests · clippyWarnings
        git show main:.claude/CLAUDE.md            # the refresh-status.sh block
      cratesTarget  ADR-037 §5: "40-42 -> 50-90 crates (cap 100 unchanged)".
        git show main:docs/adr/adr-037-bottom-up-diamond-progression.md
        A horizon, not a denominator: the count is projected, never a gate.
      adrs / adrsAccepted / adrsProposed
        git ls-tree -r --name-only main -- docs/adr/ | grep -cE 'adr-[0-9]+.*\.md$'
        ...then grep '^status:' in each (53 accepted / 12 proposed / 1 superseded /
        1 rejected = 67).
      providers        git show main:crates/nika-catalog/data/llm-providers.toml \
                         | grep -cE '^\[\[providers\]\]$'
      capabilityRules  git show main:crates/nika-catalog/data/model-capabilities.toml \
                         | grep -cE '^\[\[rules\]\]$'
      hygieneVectors   git show main:scripts/hygiene/check-all.sh | grep -cE '^run_check '

    DELIBERATELY ABSENT: hygieneGreen / hygieneYellow / hygieneRed. They were
    hard-coded literals that no script ever refreshed, they never summed to
    hygieneVectors, and they are not derivable without running the engine's
    `scripts/hygiene/check-all.sh --format=json`. A per-vector pass/fail breakdown
    is a live CI fact, not a docs constant. Do not re-add them from memory.

    Note `providers` here (38) is the EXTENDED TOML catalog record count, a
    different noun from CANON.providers (17 canonical language providers in
    _canon.mdx). Both are correct; never swap one for the other.

    Regenerate the two release fields: bash scripts/mintlify-snapshot.sh */}

export const STATUS = {
  head: "79588ad76",
  branch: "main",
  version: "0.113.0",
  cratesWorkspace: 56,
  cratesAdmitted: 54,
  cratesTarget: "50-90",
  cratesCap: 100,
  wipCrates: ["nika-chart", "nika-fx"],
  libTests: 4809,
  clippyWarnings: 0,
  adrs: 67,
  adrsAccepted: 53,
  adrsProposed: 12,
  providers: 38,
  capabilityRules: 49,
  hygieneVectors: 40,
  lastUpdated: "2026-08-22"
};
