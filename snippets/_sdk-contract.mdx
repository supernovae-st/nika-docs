{/* _sdk-contract.mdx: one source for the One SDK release horizon.
    Pass SDK explicitly as sdk: imported components must not rely on sibling
    variable exports being in scope. Keep availability separate from
    the source and engine contract. */}

export const SDK = {
  package: "@supernovae-st/nika-client",
  source: "https://github.com/supernovae-st/nika-client",
  sourceVersion: "0.118.2",
  sourceCandidate: "https://github.com/supernovae-st/nika-client/pull/86",
  engineCandidate: "https://github.com/supernovae-st/nika/commit/433e8d4ca48b9363cf5f4af9a53ab83006088fed",
  site: "https://nika.sh/sdk",
}

export const SourceContract = ({ sdk }) => (
  <Warning>
    These pages describe the pending <code>{sdk.sourceVersion}</code> SDK source
    candidate in the <a href={sdk.sourceCandidate}>SDK pull request</a> and its
    matching <a href={sdk.engineCandidate}>exact engine source</a>. Neither candidate
    is published. The merged source SHA and public package artifacts must be
    pinned before these examples become a released installation contract.
  </Warning>
)

export const LocalContract = ({ sdk }) => (
  <Tip>
    <strong>One facade, local by default.</strong> Import <code>Nika</code> from
    the package root and omit <code>url</code>. The client resolves the engine
    from <code>bin</code>, then <code>NIKA_BIN</code>, then the exact optional
    platform package. It deliberately refuses an implicit <code>PATH</code>
    {" "}lookup. These examples target the pending
    <a href={sdk.sourceCandidate}> {sdk.sourceVersion} source candidate</a>.
  </Tip>
)

export const RemoteContract = ({ sdk }) => (
  <Info>
    <strong>The same facade selects HTTP when <code>url</code> and
    {" "}<code>token</code> are present.</strong> Contained served names go directly
    to the resident and need no local binary or source. Explicit local paths
    such as <code>./workflow.nika.yaml</code> require a compatible local engine
    to capture an immutable snapshot. The candidate OpenAPI includes jobs,
    SSE, cancellation, trace diagnostics,
    workflows and schedule read/write. It does not include
    {" "}<code>POST /v1/run</code>, artifact routes, job listing, or schedule
    list/delete/trigger/backfill routes. These examples target the pending
    <a href={sdk.sourceCandidate}> {sdk.sourceVersion} source candidate</a>;
    its npm package and matching engine release are not yet published.
  </Info>
)

export const SdkDoors = () => (
  <CardGroup cols={3}>
    <Card title="Start" icon="play" href="/sdk/start/quickstart">
      Check registry availability and complete one typed run.
    </Card>
    <Card title="Project" icon="folder-tree" href="/sdk/project/nika-yaml">
      nika.yaml, arm policy, precedence and runtime state.
    </Card>
    <Card title="Local API" icon="terminal" href="/sdk/local/client">
      Check, run, observe, cancel and verify through the local engine.
    </Card>
    <Card title="Runtime" icon="wave-pulse" href="/sdk/runtime/events">
      Admission, events, terminal results and durable receipts.
    </Card>
    <Card title="Remote API" icon="satellite-dish" href="/sdk/remote/client">
      Authenticated jobs, recovery, SSE, workflows and schedules.
    </Card>
    <Card title="Operations" icon="shield-halved" href="/sdk/operations/ci">
      Admission, resident serving, OS bridges and security.
    </Card>
    <Card title="Reference" icon="brackets-curly" href="/sdk/reference/methods">
      Constructor, methods, errors and exported types.
    </Card>
  </CardGroup>
)
